{
  "name": "mythical-ai",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev-frontend": "npm run dev --prefix frontend",
    "dev-backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run dev-frontend\" \"npm run dev-backend\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend --if-present",
    "lint": "npm run lint --workspace frontend --if-present && npm run lint --workspace backend --if-present",
    "setup": "cp .env.example .env"
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
